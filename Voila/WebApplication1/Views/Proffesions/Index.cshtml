@model IEnumerable<WebApplication1.Proffesion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProffesionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProffesionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProffesionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProffesionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProffesionId })
        </td>
    </tr>
}

</table>
